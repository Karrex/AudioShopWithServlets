buildscript {
    sourceSets {
        main {
            java {
                srcDir 'src/java'
            }
        }
    }
}
dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'javax.servlet:servlet-api:2.5'
}
